name: Scheduled crons

# Free-tier friendly scheduler: Vercel Hobby caps cron jobs at ~once/day, so we
# drive the cron endpoints from GitHub Actions instead. The endpoints require
# Authorization: Bearer ${CRON_SECRET}.
on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: scheduled-crons
  cancel-in-progress: false

jobs:
  run-crons:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    env:
      BASE_URL: ${{ vars.APP_BASE_URL || 'https://www.agendita.cl' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      - name: Expire stale holds
        id: expire_holds
        continue-on-error: true
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: scripts/run-json-cron.sh "${BASE_URL%/}/api/cron/expire-holds"
      - name: Send reminders
        id: send_reminders
        continue-on-error: true
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: scripts/run-json-cron.sh "${BASE_URL%/}/api/cron/send-reminders"
      - name: Transfer reminders
        id: transfer_reminders
        continue-on-error: true
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: scripts/run-json-cron.sh "${BASE_URL%/}/api/cron/transfer-reminders"
      - name: Loyalty automatic conditions
        id: loyalty_automatic
        continue-on-error: true
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: scripts/run-json-cron.sh "${BASE_URL%/}/api/cron/loyalty-automatic"
      - name: Subscription billing
        id: subscription_billing
        continue-on-error: true
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: scripts/run-json-cron.sh "${BASE_URL%/}/api/cron/subscription-billing"
      - name: Fail if any cron failed
        if: always()
        env:
          EXPIRE_HOLDS_OUTCOME: ${{ steps.expire_holds.outcome }}
          SEND_REMINDERS_OUTCOME: ${{ steps.send_reminders.outcome }}
          TRANSFER_REMINDERS_OUTCOME: ${{ steps.transfer_reminders.outcome }}
          LOYALTY_AUTOMATIC_OUTCOME: ${{ steps.loyalty_automatic.outcome }}
          SUBSCRIPTION_BILLING_OUTCOME: ${{ steps.subscription_billing.outcome }}
        run: |
          for outcome in \
            "$EXPIRE_HOLDS_OUTCOME" \
            "$SEND_REMINDERS_OUTCOME" \
            "$TRANSFER_REMINDERS_OUTCOME" \
            "$LOYALTY_AUTOMATIC_OUTCOME" \
            "$SUBSCRIPTION_BILLING_OUTCOME"
          do
            if [ "$outcome" != "success" ]; then
              exit 1
            fi
          done
