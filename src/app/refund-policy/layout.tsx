import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Política de reembolsos' }
export default function RefundPolicyLayout({ children }: { children: React.ReactNode }) { return children }
