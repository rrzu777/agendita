import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Recuperar negocio' }
export default function RecoverBusinessLayout({ children }: { children: React.ReactNode }) { return children }
